# Augment treedata with registry annotations

Augment treedata with registry annotations

## Usage

``` r
augment_treedata_with_registry(treedata_obj, registry)
```

## Arguments

- treedata_obj:

  A treedata object

- registry:

  A registry data frame
