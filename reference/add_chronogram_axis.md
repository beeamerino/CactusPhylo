# Add chronogram axis

Add chronogram axis

## Usage

``` r
add_chronogram_axis(
  p,
  tree,
  by = 5,
  digits = 0L,
  axis_offset_frac = 0.052,
  tick_height_frac = 0.01,
  label_offset_frac = 0.028,
  title_margin_top = 28,
  bar_size = 12,
  segment_size = 2.5
)
```
