# Run treePL directly

Run treePL directly

## Usage

``` r
run_treePL_direct(cfg_file, label, cwd = NULL)
```

## Arguments

- cfg_file:

  Character. Configuration file.

- label:

  Character. Label.

- cwd:

  Character. Working directory.
